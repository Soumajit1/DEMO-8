class Solution {
public:
    bool isValid(string s) {
        s
        return stk.empty();
    }
   bool match(char l, char r) {
        return (l == '(' && r == ')') || (l == '[' && r == ']') || (l == '{' && r == '}');GH,J   }
};
 25.08.2026
